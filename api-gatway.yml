logging:
  level:
    org:
      springframework:
        cloud:
          gateway:
            route:
              RouteDefinitionRouteLocator: INFO
    root: INFO
management:
  endpoints:
    web:
      exposure:
        include: prometheus
zuul:
  routes:
    user-service:
     path: localhost:6666/user-service/**
     service-id: user-service
    task-service:
     path: localhost:6666/task-service/**
     service-id: task-service
     
#eureka 
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

